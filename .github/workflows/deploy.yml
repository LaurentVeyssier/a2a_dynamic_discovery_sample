name: Azure Deploy

on:
  push:
    branches:
      - main  # Make sure this matches your branch name

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment:
      name: production
      url: https://a2a-discovery-demo-app-bnhvaveuepb4b5f9.francecentral-01.azurewebsites.net
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Log in to Azure
        uses: azure/login@v2 # Uses the Secret you created earlier
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'a2a-discovery-demo-app'
          package: .